package tests;

import org.testng.Assert;
import org.testng.annotations.Test;
import pages.HomePage;

public class CheckoutTests extends BaseTest{

/*    @Test
    public void sampleTest(){
        HomePage homePage = new HomePage(driver);
        String actual = homePage.changeLanguage().selectProductItem()
                .clickAddToBasket().proceedToCheckOut()
                .setDeliveryOptions("Cairo","Ain Shams").setAddressDetails()
                .clickContinue().getAlertText();
        Assert.assertEquals(actual, "Please enter a valid name\n");
    }*/
}
